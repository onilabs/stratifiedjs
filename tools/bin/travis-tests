#!/bin/bash
set -eux
# (this file should be run from the repository root)
suite="test/run.html"
if [ "$RUNNER" = node ]; then
	./apollo "$suite"
else
	[ "$RUNNER" = karma ];
	export PHANTOMJS_BIN=phantomjs
	./karma.sjs --browsers=PhantomJS --single-run run -- "$suite"
fi
